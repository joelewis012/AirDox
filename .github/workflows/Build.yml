name: Build AirDox FAP

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Clone official Flipper firmware
        run: |
          git clone https://github.com/flipperdevices/flipperzero-firmware.git

      - name: Copy AirDox app into firmware
        run: |
          mkdir -p flipperzero-firmware/applications_user/air_dox
          cp AirDox/applications_user/air_dox/* flipperzero-firmware/applications_user/air_dox/

      - name: Build AirDox FAP
        run: |
          cd flipperzero-firmware
          ./fbt fap_air_dox

      - name: Upload FAP artifact
        uses: actions/upload-artifact@v4
        with:
          name: air_dox_fap
          path: flipperzero-firmware/dist/*.fap
